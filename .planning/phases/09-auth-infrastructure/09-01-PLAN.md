---
phase: 09-auth-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/tools/__tests__/LockedToolShell.test.tsx
  - src/components/tools/LockedToolShell.tsx
  - src/__tests__/LoginPage.test.tsx
autonomous: true
requirements:
  - PERM-01
  - PERM-02
  - PERM-03
must_haves:
  truths:
    - "LockedToolShell.test.tsx exists with it.todo() stubs covering blocked-state render, heading copy, body copy, and lock icon"
    - "LockedToolShell.tsx stub file exists so jest.mock resolution succeeds"
    - "LoginPage.test.tsx has it.todo() stubs for team login path (PERM-02) and individual login path (PERM-03)"
    - "npx jest --passWithNoTests exits 0 after Wave 0 (no failing tests)"
  artifacts:
    - path: "src/components/tools/__tests__/LockedToolShell.test.tsx"
      provides: "Nyquist stubs for PERM-01 blocked-state tests"
    - path: "src/components/tools/LockedToolShell.tsx"
      provides: "Stub file enabling jest.mock resolution"
    - path: "src/__tests__/LoginPage.test.tsx"
      provides: "Extended stubs for PERM-02 and PERM-03 login paths"
  key_links:
    - from: "src/components/tools/__tests__/LockedToolShell.test.tsx"
      to: "src/components/tools/LockedToolShell.tsx"
      via: "import LockedToolShell"
      pattern: "import.*LockedToolShell"
---

<objective>
Wave 0: Create Nyquist test stubs for Phase 9 so Wave 1 and Wave 2 implementations have pre-existing test contracts to fill.

Purpose: Per project Nyquist Rule, every file that will have tests must have it.todo() stubs committed BEFORE implementation. This plan produces zero real tests — only stubs that compile, pass, and define what the real tests will verify.
Output: Three files with it.todo() stubs. Full suite still green.
</objective>

<execution_context>
@C:/Users/power/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/power/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-auth-infrastructure/09-VALIDATION.md
@.planning/phases/09-auth-infrastructure/09-UI-SPEC.md

<interfaces>
<!-- Current LockedToolShell.tsx does not exist — stub must be created -->
<!-- LoginPage.test.tsx exists at src/__tests__/LoginPage.test.tsx -->
<!-- AgentShell props (for LockedToolShell stub context): -->
<!--   title: string, description?: string, statusBadge?: React.ReactNode, children: React.ReactNode -->

From src/components/tools/AgentShell.tsx:
```typescript
interface AgentShellProps {
  title: string
  description?: string
  statusBadge?: React.ReactNode
  children: React.ReactNode
  className?: string
}
export function AgentShell(props: AgentShellProps): JSX.Element
```

From src/__tests__/LoginPage.test.tsx (existing file to extend):
<!-- Read before touching — file may already have tests that must not be broken -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create LockedToolShell stub file + test stubs (PERM-01)</name>
  <files>
    src/components/tools/LockedToolShell.tsx
    src/components/tools/__tests__/LockedToolShell.test.tsx
  </files>
  <read_first>
    - src/components/tools/AgentShell.tsx (layout wrapper to use in stub)
    - src/__tests__/AreaCard.test.tsx (it.todo stub pattern for a tools component)
    - src/components/tools/AreaCard.tsx (minimal stub pattern reference)
  </read_first>
  <action>
**Step 1 — Create LockedToolShell.tsx stub:**

Create `src/components/tools/LockedToolShell.tsx` as a minimal stub so Jest module resolution succeeds. The real implementation comes in Plan 02.

```typescript
// src/components/tools/LockedToolShell.tsx
// Stub — Wave 0. Real implementation in Plan 02 (09-02-PLAN.md).

interface LockedToolShellProps {
  toolName: string
}

export function LockedToolShell({ toolName: _toolName }: LockedToolShellProps) {
  return null
}
```

**Step 2 — Create test file with it.todo() stubs:**

Create `src/components/tools/__tests__/LockedToolShell.test.tsx` with stubs matching VALIDATION.md requirements. Do NOT write `import React` — the project uses the JSX transform. DO mock AgentShell as other test files in the project mock it (check src/__tests__/AgentShell.test.tsx for the pattern).

```typescript
// src/components/tools/__tests__/LockedToolShell.test.tsx
import { render, screen } from '@testing-library/react'
import { LockedToolShell } from '../LockedToolShell'

jest.mock('@/components/tools/AgentShell', () => ({
  AgentShell: ({ children }: { children: React.ReactNode }) => (
    <div data-testid="agent-shell">{children}</div>
  ),
}))

describe('LockedToolShell', () => {
  it.todo('renders inside AgentShell wrapper')
  it.todo('displays "Acesso restrito" heading')
  it.todo('displays "Você não tem acesso a esta ferramenta. Contate o administrador." body text')
  it.todo('renders LockKeyhole icon with aria-hidden="true"')
  it.todo('heading is an h2 element')
})
```

Note: The it.todo() stubs are placeholders only — they do NOT import `render` or `screen` in the actual calls (it.todo takes only a string). The imports at the top are included now so they do not need to be added in Plan 02.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="LockedToolShell" --passWithNoTests --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/tools/LockedToolShell.tsx` exists and contains `export function LockedToolShell`
    - `src/components/tools/__tests__/LockedToolShell.test.tsx` exists
    - File contains exactly 5 `it.todo(` calls
    - `it.todo('displays "Acesso restrito" heading')` is present verbatim
    - `it.todo('displays "Você não tem acesso a esta ferramenta. Contate o administrador." body text')` is present verbatim
    - `npx jest --testPathPattern="LockedToolShell" --passWithNoTests --no-coverage` exits 0 with 0 failures
  </acceptance_criteria>
  <done>LockedToolShell stub + 5 it.todo() test stubs committed; jest passes with no failures.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend LoginPage.test.tsx with PERM-02 and PERM-03 stubs</name>
  <files>
    src/__tests__/LoginPage.test.tsx
  </files>
  <read_first>
    - src/__tests__/LoginPage.test.tsx (read ENTIRE file before making any change — preserve existing tests verbatim)
    - src/app/login/LoginForm.tsx (understand current form structure for stub naming)
    - src/__tests__/ToolCard.test.tsx (it.todo stub pattern for an existing test file that was extended)
  </read_first>
  <action>
Read `src/__tests__/LoginPage.test.tsx` fully first. Then APPEND a new describe block at the bottom of the file — do NOT modify existing tests.

Add this block at the end of the file:

```typescript
describe('LoginForm — individual account toggle (PERM-02, PERM-03)', () => {
  it.todo('team mode is the default — only password field visible, email field not rendered')
  it.todo('team mode uses TEAM_EMAIL (team@flyingstudio.com.br) constant in signInWithPassword call')
  it.todo('clicking "Entrar com conta individual" toggles to individual mode')
  it.todo('individual mode renders email field with type="email" and aria-label="E-mail"')
  it.todo('individual mode subheading changes to "Digite seu e-mail e senha para continuar"')
  it.todo('individual mode uses user-supplied email (not TEAM_EMAIL) in signInWithPassword call')
  it.todo('clicking "Usar senha da equipe" returns to team mode — email field hidden again')
  it.todo('individual mode error shows "E-mail ou senha incorretos. Tente novamente."')
  it.todo('team mode error shows "Senha incorreta. Tente novamente."')
})
```

Preserve everything above this block exactly as it was. Do not reformat, reorganize, or touch any existing test.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="LoginPage" --passWithNoTests --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/LoginPage.test.tsx` contains `describe('LoginForm — individual account toggle (PERM-02, PERM-03)'`
    - File contains exactly 9 new `it.todo(` calls inside that describe block
    - `it.todo('team mode uses TEAM_EMAIL (team@flyingstudio.com.br) constant in signInWithPassword call')` is present verbatim
    - `it.todo('individual mode uses user-supplied email (not TEAM_EMAIL) in signInWithPassword call')` is present verbatim
    - `npx jest --testPathPattern="LoginPage" --passWithNoTests --no-coverage` exits 0 with 0 failures
    - No previously passing tests in LoginPage.test.tsx now fail
  </acceptance_criteria>
  <done>LoginPage.test.tsx extended with 9 PERM-02/PERM-03 stubs; full test file still green.</done>
</task>

</tasks>

<verification>
After both tasks:
- `npx jest --no-coverage --passWithNoTests` exits 0
- No previously passing tests are now failing (regression check)
- `src/components/tools/LockedToolShell.tsx` exports `LockedToolShell` function
- `src/components/tools/__tests__/LockedToolShell.test.tsx` has 5 it.todo() stubs
- `src/__tests__/LoginPage.test.tsx` has new describe block with 9 it.todo() stubs
</verification>

<success_criteria>
- Wave 0 stubs committed for all Phase 9 test files
- Full Jest suite green (0 failures)
- Ready for Plan 02 (Wave 1) to implement LockedToolShell and config changes
</success_criteria>

<output>
After completion, create `.planning/phases/09-auth-infrastructure/09-01-SUMMARY.md`
</output>
