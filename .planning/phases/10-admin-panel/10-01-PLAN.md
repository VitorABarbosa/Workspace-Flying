---
phase: 10-admin-panel
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/middleware.test.ts
  - src/app/admin/__tests__/AdminPage.test.tsx
  - src/components/admin/__tests__/MemberList.test.tsx
  - src/components/admin/__tests__/CreateMemberForm.test.tsx
  - src/components/admin/__tests__/EditPermissionsForm.test.tsx
  - src/components/admin/__tests__/MemberRow.test.tsx
autonomous: true
requirements:
  - PERM-06
  - PERM-07
  - PERM-08
  - PERM-09
  - PERM-10

must_haves:
  truths:
    - "All Wave 0 test files exist and Jest runs them with 0 failures (all stubs are it.todo)"
    - "middleware.test.ts has admin-specific describe block appended without touching existing AUTH-02 tests"
    - "No component implementation files are created — stubs are test contracts only"
  artifacts:
    - path: "src/__tests__/middleware.test.ts"
      provides: "PERM-06 admin guard stubs appended"
    - path: "src/app/admin/__tests__/AdminPage.test.tsx"
      provides: "PERM-06 admin page render + PERM-07 member list stubs"
    - path: "src/components/admin/__tests__/MemberList.test.tsx"
      provides: "PERM-07 member list rendering stubs"
    - path: "src/components/admin/__tests__/CreateMemberForm.test.tsx"
      provides: "PERM-08 create member form stubs"
    - path: "src/components/admin/__tests__/EditPermissionsForm.test.tsx"
      provides: "PERM-09 edit permissions form stubs"
    - path: "src/components/admin/__tests__/MemberRow.test.tsx"
      provides: "PERM-10 disable/delete member stubs"
  key_links:
    - from: "src/components/admin/__tests__/MemberList.test.tsx"
      to: "src/components/admin/MemberList.tsx"
      via: "jest.mock placeholder — file does not exist yet"
      pattern: "it\\.todo"
    - from: "src/__tests__/middleware.test.ts"
      to: "src/middleware.ts"
      via: "describe block append"
      pattern: "admin"
---

<objective>
Wave 0: create all test stub files for Phase 10 before any implementation begins.

Purpose: Nyquist Rule compliance — tests exist as contracts before code is written. All stubs use `it.todo()` so Jest runs them with 0 failures and 0 passes.
Output: 5 new test files + 1 extended test file, all passing (no failures) with `--passWithNoTests`.
</objective>

<execution_context>
@C:/Users/power/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/power/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-admin-panel/10-RESEARCH.md
@.planning/phases/10-admin-panel/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend middleware.test.ts with PERM-06 admin guard stubs</name>
  <files>src/__tests__/middleware.test.ts</files>
  <read_first>
    - src/__tests__/middleware.test.ts — read current file in full; append-only, never touch existing describe block
  </read_first>
  <action>
Append a new `describe` block to the END of `src/__tests__/middleware.test.ts`. Do NOT modify any existing code in the file.

Append exactly this block:

```typescript
describe('middleware — proteção de rotas admin (PERM-06)', () => {
  it.todo('redireciona role=member de /admin para /tools')
  it.todo('redireciona usuário não autenticado de /admin para /login')
  it.todo('permite role=admin acessar /admin sem redirecionamento')
  it.todo('matcher config inclui /admin/:path*')
})
```

No new imports needed — the existing mocks and helpers at the top of the file apply to all describe blocks.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="middleware" --no-coverage --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File contains the string `describe('middleware — proteção de rotas admin (PERM-06)'`
    - File contains exactly 4 `it.todo` entries in the new block: `redireciona role=member`, `redireciona usuário não autenticado de /admin`, `permite role=admin`, `matcher config inclui /admin/:path*`
    - Existing `describe('middleware — proteção de rotas (AUTH-02)'` block is unchanged (grep still finds all 4 original test names)
    - `npx jest --testPathPattern="middleware" --no-coverage --passWithNoTests` exits 0 with no failures
  </acceptance_criteria>
  <done>middleware.test.ts contains the new PERM-06 describe block appended after the AUTH-02 block; Jest passes</done>
</task>

<task type="auto">
  <name>Task 2: Create 5 admin component test stub files</name>
  <files>
    src/app/admin/__tests__/AdminPage.test.tsx,
    src/components/admin/__tests__/MemberList.test.tsx,
    src/components/admin/__tests__/CreateMemberForm.test.tsx,
    src/components/admin/__tests__/EditPermissionsForm.test.tsx,
    src/components/admin/__tests__/MemberRow.test.tsx
  </files>
  <read_first>
    - src/__tests__/middleware.test.ts — understand the project's stub pattern (it.todo, no imports of non-existent modules)
    - src/components/tools/__tests__/ — check for any existing component stub patterns if present
  </read_first>
  <action>
Create the following 5 files. Each file uses `it.todo()` stubs ONLY — no imports of component files (those do not exist yet), no jest.mock of non-existent modules. This avoids compile errors.

**File 1: `src/app/admin/__tests__/AdminPage.test.tsx`**
```typescript
// AdminPage.test.tsx — stubs para PERM-06 (admin page guard) e PERM-07 (member list)
// Wave 0: it.todo() contracts — implementation in Wave 1 (plan 10-02)

describe('AdminPage — servidor renderiza lista de membros (PERM-07)', () => {
  it.todo('renderiza heading "Painel Admin" na página')
  it.todo('exibe a lista de membros quando usuário é admin')
  it.todo('cada membro exibe nome, email, role e ferramentas autorizadas')
})
```

**File 2: `src/components/admin/__tests__/MemberList.test.tsx`**
```typescript
// MemberList.test.tsx — stubs para PERM-07 (member list rendering)
// Wave 0: it.todo() contracts — implementation in Wave 1 (plan 10-02)

describe('MemberList — renderização da lista de membros (PERM-07)', () => {
  it.todo('renderiza uma linha por membro passado como prop')
  it.todo('exibe nome do membro em cada linha')
  it.todo('exibe email do membro em cada linha')
  it.todo('exibe role do membro (admin ou member) em cada linha')
  it.todo('exibe ferramentas autorizadas do membro em cada linha')
  it.todo('exibe badge "Desativado" para membro com banned_until preenchido')
  it.todo('renderiza mensagem vazia quando lista de membros está vazia')
})
```

**File 3: `src/components/admin/__tests__/CreateMemberForm.test.tsx`**
```typescript
// CreateMemberForm.test.tsx — stubs para PERM-08 (create member)
// Wave 0: it.todo() contracts — implementation in Wave 2 (plan 10-03)

describe('CreateMemberForm — criação de membro (PERM-08)', () => {
  it.todo('renderiza campos: nome, email, senha, role e ferramentas')
  it.todo('submeter formulário chama createMember com nome, email, senha, role e ferramentas selecionadas')
  it.todo('checkboxes de ferramenta refletem as ferramentas disponíveis em tools.ts')
  it.todo('exibe erro inline se email já existir (erro retornado pela action)')
  it.todo('limpa o formulário após criação bem-sucedida')
})
```

**File 4: `src/components/admin/__tests__/EditPermissionsForm.test.tsx`**
```typescript
// EditPermissionsForm.test.tsx — stubs para PERM-09 (edit permissions)
// Wave 0: it.todo() contracts — implementation in Wave 2 (plan 10-03)

describe('EditPermissionsForm — edição de permissões (PERM-09)', () => {
  it.todo('renderiza um checkbox por ferramenta em tools.ts')
  it.todo('checkboxes pré-marcados refletem as ferramentas já autorizadas do membro')
  it.todo('submeter chama updatePermissions com userId e array de slugs marcados')
  it.todo('submeter com nenhuma ferramenta marcada chama updatePermissions com array vazio')
  it.todo('role select pré-selecionado com role atual do membro')
  it.todo('submeter atualiza role via updatePermissions')
})
```

**File 5: `src/components/admin/__tests__/MemberRow.test.tsx`**
```typescript
// MemberRow.test.tsx — stubs para PERM-10 (disable/delete member)
// Wave 0: it.todo() contracts — implementation in Wave 2 (plan 10-03)

describe('MemberRow — ações de membro (PERM-10)', () => {
  it.todo('renderiza botão "Desativar" para membro ativo')
  it.todo('clicar em "Desativar" chama disableMember com userId do membro')
  it.todo('renderiza botão "Reativar" para membro com banned_until preenchido')
  it.todo('renderiza botão "Remover permanentemente" que requer confirmação')
  it.todo('confirmar remoção chama deleteMember com userId do membro')
  it.todo('cancelar confirmação não chama deleteMember')
})
```

Create all parent directories as needed (`src/app/admin/__tests__/` and `src/components/admin/__tests__/`).
  </action>
  <verify>
    <automated>npx jest --testPathPattern="admin" --no-coverage --passWithNoTests 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/admin/__tests__/AdminPage.test.tsx` exists and contains `describe('AdminPage`
    - `src/components/admin/__tests__/MemberList.test.tsx` exists and contains `describe('MemberList`
    - `src/components/admin/__tests__/CreateMemberForm.test.tsx` exists and contains `describe('CreateMemberForm`
    - `src/components/admin/__tests__/EditPermissionsForm.test.tsx` exists and contains `describe('EditPermissionsForm`
    - `src/components/admin/__tests__/MemberRow.test.tsx` exists and contains `describe('MemberRow`
    - `npx jest --testPathPattern="admin" --no-coverage --passWithNoTests` exits 0 with no failures (only todo tests)
    - No file imports a component path that does not yet exist
  </acceptance_criteria>
  <done>5 stub files created in the correct directories; Jest runs them all with 0 failures</done>
</task>

</tasks>

<verification>
Full Wave 0 verification:

```bash
npx jest --testPathPattern="admin|middleware" --no-coverage --passWithNoTests
```

Expected: 0 failures, all stubs reported as todo.

Check files exist:
```bash
ls src/app/admin/__tests__/AdminPage.test.tsx
ls src/components/admin/__tests__/MemberList.test.tsx
ls src/components/admin/__tests__/CreateMemberForm.test.tsx
ls src/components/admin/__tests__/EditPermissionsForm.test.tsx
ls src/components/admin/__tests__/MemberRow.test.tsx
grep "admin (PERM-06)" src/__tests__/middleware.test.ts
```
</verification>

<success_criteria>
- 6 test files exist (5 new + 1 extended) with 0 failures
- No component implementation files created
- All stubs use it.todo() — no broken imports of non-existent modules
- `npx jest --testPathPattern="admin|middleware" --no-coverage --passWithNoTests` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-panel/10-01-SUMMARY.md`
</output>
