// src/components/tools/LockedToolShell.tsx
// Stub — Wave 0. Real implementation in Plan 02 (09-02-PLAN.md).

interface LockedToolShellProps {
  toolName: string
}

export function LockedToolShell({ toolName: _toolName }: LockedToolShellProps) {
  return null
}
